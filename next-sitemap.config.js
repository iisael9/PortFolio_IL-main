/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://port-folio-il-main.vercel.app/', // ← CHANGE this to your real domain
    generateRobotsTxt: true, // Optional but recommended
}
  